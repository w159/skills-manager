# Memory

Durable, non-obvious facts about this codebase. One line per fact; each links to a file in
this directory. The SessionStart hook loads this index automatically. Format:
`.agents/memory/<slug>.md` (see the codebase-brain skill's references/memory-format.md).

<!-- - [example-decision](example-decision.md) — why we use X over the obvious Y -->
